#ifndef GAME_STATE_H
#define GAME_STATE_H

enum class GameState
{
	SPLASH_STATE,
	MENU_STATE,
	PLAY_STATE,
	UPGRADE_STATE,
	OPTIONS_STATE,
	CREDITS_STATE,
	PRERACE_STATE,
	END_STATE
};
#endif // !GAME_STATE_H